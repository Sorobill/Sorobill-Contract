# Export checklist

Compare `docs/EXPORTS.md` with `contracts/sorobill/src/lib.rs` before release.

Cross-check against `docs/EXPORTS.md` and `clients/typescript/src/types.ts`
`METHODS` whenever entrypoints change.
