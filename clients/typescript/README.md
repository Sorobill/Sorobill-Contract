# @sorobill/contract-types

Stub TypeScript types for Sorobill 0.3.0 until `stellar contract bindings`
are regenerated against the new wasm.

## Contents

- `src/types.ts` — `BillingInterval`, `Plan`, `Subscription`, `BillingOutcome`
- Constants: `CONTRACT_VERSION`, `DEFAULT_GRACE_SECS`, `MAX_FAILED_ATTEMPTS`
- `METHODS` — exhaustive entrypoint name list for cross-checks

## Regenerate later

See `docs/BINDINGS.md`. Keep stubs in sync when adding contract methods.
